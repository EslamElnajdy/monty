#include "monty.h"
/**
  *f_nop- don,t do any thing
  *@head: pointer  to atack
  *@line_number: number of line.
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int line_number)
{
	(void) line_number;
	(void) head;
}
